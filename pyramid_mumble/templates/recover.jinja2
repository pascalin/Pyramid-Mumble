{% extends "layout.jinja2" %}

{% block header %}
    <div class="header row">
        <div class="col text-center">
             <h2><span class="font-semi-bold">Recover your password</h2>
        </div>
    </div>
{% endblock %}

{% block content %}
    <div class="content row">
    <div class="col-md-3"></div>
        <div class="col-md-8">
          {% if recover_form %}
             {{ recover_form|safe }}
          {% endif %}
          {% if appstruct %}
          {{ appstruct }}
          {% endif %}
        </div>
    </div>
{% endblock content %}
